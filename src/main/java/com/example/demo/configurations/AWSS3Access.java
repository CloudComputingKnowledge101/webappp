package com.example.demo.configurations;

import org.springframework.context.annotation.Configuration;

@Configuration
public class AWSS3Access {


	/*
	 * @Bean AmazonS3 getAmazonS3Client() { final BasicAWSCredentials
	 * basicAWSCredentials = new BasicAWSCredentials(accessKeyId, accessKeySecret);
	 * 
	 * System.out.println("AccessID: " + accessKeyId + " SECRET: " + accessKeySecret
	 * + " REGION: " + s3RegionName);
	 * 
	 * // Get Amazon S3 client and return the S3 client object return
	 * AmazonS3ClientBuilder .standard() .withCredentials(new
	 * AWSStaticCredentialsProvider(basicAWSCredentials)) .withRegion(s3RegionName)
	 * .build(); }
	 */
}